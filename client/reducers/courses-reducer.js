const initialState = [
  {
    id: '5',
    name: 'My Course',
    description: 'Nice course'
  }
];

const coursesContext = (state = initialState, action) => {
  return state;
};

export { coursesContext };
